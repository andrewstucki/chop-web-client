import styled from 'styled-components';
import type { ComponentType } from 'react';
import type { NoPropsType } from '../../cwc-types';

export const TimezoneSelectorWrapper:ComponentType<NoPropsType> = styled.div`
  margin-left: auto;
  margin-right: 24px;
`;
